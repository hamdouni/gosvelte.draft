version: '3'
services:
    web:
        build:
            dockerfile: ./Dockerfile
            context: ./
        volumes:
            - ./ui/web/static:/cmd/web/static
        command: /server -host=0.0.0.0 -port=80 -static=/cmd/web/static
        ports:
            - 80:80
